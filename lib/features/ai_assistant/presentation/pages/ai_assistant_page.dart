import 'package:flutter/material.dart';
import 'package:fashion_store/core/widgets/placeholder_page.dart';

/// Asistente inteligente (Gemini vía FastAPI) y recomendaciones
/// personalizadas. Requiere sesión iniciada. Contenido real en las
/// Fases 18 y 19.
class AiAssistantPage extends StatelessWidget {
  const AiAssistantPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderPage(title: 'Asistente FashionStore');
  }
}
